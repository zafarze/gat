🚀 Инструкция по обновлению сервера GAT (v2.0: Smart Notifications & Question Bank)
Дата обновления: 19.11.2025 Статус: Critical Update (Изменения в БД и логике ядра)

📋 Что нового?
Центр Вопросов (Question Bank): Полная поддержка базы вопросов с картинками, темами и уровнями сложности.

Умная рассылка (Smart Notifications): Система уведомлений теперь изолирует данные.

Директор видит уведомления только своей школы.

Учитель видит уведомления только своей школы.

Суперадмин/Эксперт видят всё.

Генерация буклетов: Автоматическая верстка тестов для печати.

Аккаунты: Массовая генерация логинов/паролей с экспортом в PDF.

⚠️ ШАГ 0: БЭКАП (ОБЯЗАТЕЛЬНО!)
Перед любыми действиями сохраните базу данных. Если что-то пойдет не так, мы сможем откатиться.

Bash

# Если вы используете PostgreSQL (рекомендуется):
pg_dump -U postgres -d gat_db > gat_backup_$(date +%F).sql

# Если вы используете SQLite (просто скопируйте файл):
cp db.sqlite3 db.sqlite3.backup
🛠 ШАГ 1: Обновление кода и зависимостей
Затяните свежий код из репозитория:

Bash

git pull origin main
(Убедитесь, что у вас последние версии файлов core/views/reports_upload.py и accounts/views.py с исправленными импортами!)

Активируйте виртуальное окружение:

Bash

# Windows
.\venv\Scripts\activate
# Linux/Mac
source venv/bin/activate
Установите новые библиотеки: Мы добавили Pillow для работы с изображениями.

Bash

pip install -r requirements.txt
🗄️ ШАГ 2: Миграции Базы Данных
Так как мы добавили модели Notification, BankQuestion и изменили связи, нужно обновить структуру БД.

Bash

python manage.py makemigrations
python manage.py migrate
Ожидаемый результат: Вы должны увидеть применение миграций для core и accounts. Если написано No migrations to apply, значит, база уже актуальна.

🎨 ШАГ 3: Статика и Медиа
Так как появились картинки в вопросах, нужно проверить настройки медиа и обновить статику.

Соберите статику (CSS/JS):

Bash

python manage.py collectstatic --noinput
Проверьте папку Media: Убедитесь, что папка media/ существует в корне проекта и у сервера есть права на запись в неё (особенно для Linux).

Bash

# Только для Linux серверов:
mkdir -p media
chmod -R 755 media
chown -R www-data:www-data media  # (или ваш пользователь сервера)
🚀 ШАГ 4: Перезапуск Сервера
Чтобы Python подхватил новый код, нужно перезапустить процесс Gunicorn (или другой WSGI сервер).

Bash

# Если используете systemd (Linux):
sudo systemctl restart gunicorn

# Если запускаете вручную (для теста):
python manage.py runserver
✅ ШАГ 5: Чек-лист проверки (Smoke Test)
После запуска зайдите на сайт под Суперадмином и выполните:

Тест уведомлений:

Зайдите в Результаты -> Upload.

Загрузите любой Excel файл с результатами.

Результат: В "колокольчике" (справа сверху) должно появиться уведомление.

Тест изоляции (Опционально):

Зайдите под аккаунтом Директора другой школы (не той, куда загрузили результаты).

Результат: У него уведомления быть не должно.

Тест картинок:

Зайдите в Банк вопросов.

Попробуйте создать вопрос и прикрепить картинку.

Результат: Вопрос создается, картинка отображается.

🆘 Если что-то пошло не так
Ошибка ImportError: cannot import name 'Notification' ...:

Вы не обновили файл accounts/views.py. Импорт Notification должен быть внутри функции profile, а не в начале файла.

Картинки не грузятся:

Проверьте, установлена ли библиотека Pillow (pip freeze | grep Pillow).

Уведомления не приходят:

Проверьте логи сервера. Если там чисто, убедитесь, что вы загружаете файл для школы, к которой привязаны пользователи (Директора/Учителя).